/**
 * Deployment-specific endpoints bundled with the desktop application.
 *
 * Change this value before packaging for a different intranet environment.
 * It is intentionally not exposed as an end-user setting.
 */
export const PROMPT_HUB_BASE_URL = 'http://localhost:8080';
